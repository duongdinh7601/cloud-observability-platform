# Frontend Dashboard Stub

This file is an older planning stub kept for historical context.

The active frontend documentation now lives at:

```text
frontend/README.md
```

The current frontend is a Next.js App Router dashboard that reads logs through the platform's same-origin `/api/logs` path. Future frontend work should be documented in the active frontend README and the main project roadmap rather than expanding this stub.
